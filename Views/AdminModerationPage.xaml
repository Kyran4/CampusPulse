<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CampusPulse.Views.AdminModerationPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="{StaticResource LightGray}">

    <Grid RowDefinitions="Auto,*">

        <Grid BackgroundColor="{StaticResource PrimaryColor}" Padding="20">
            <Label Text="Moderation" Style="{StaticResource TitleLabel}" />
        </Grid>

        <CollectionView
            Grid.Row="1"
            ItemsSource="{Binding Reports}"
            SelectionMode="None"
            Margin="20">

            <CollectionView.EmptyView>
                <Label Text="No pending reports."
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       TextColor="{StaticResource SecondaryColor}" />
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Style="{StaticResource Card}">
                        <VerticalStackLayout Spacing="10">

                            <Label Text="{Binding Reason}"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource SecondaryColor}" />

                            <Label Text="{Binding Description}"
                                   Style="{StaticResource BodyLabel}" />

                            <HorizontalStackLayout Spacing="10">
                                <Button
                                    Text="View Post"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding BindingContext.OpenPostCommand, Source={x:Reference RootPage}}"
                                    CommandParameter="{Binding PostId}" />

                                <Button
                                    Text="Action Taken"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding BindingContext.ActionTakenCommand, Source={x:Reference RootPage}}"
                                    CommandParameter="{Binding .}" />

                                <Button
                                    Text="Dismiss"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding BindingContext.DismissCommand, Source={x:Reference RootPage}}"
                                    CommandParameter="{Binding .}" />
                            </HorizontalStackLayout>

                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </Grid>
</ContentPage>
